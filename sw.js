const STATIC_CACHE='fxconvert-static-v3';const API_CACHE='fxconvert-api-v1';const STATIC_ASSETS=['./','./index.html','./style.css','./script.js','./manifest.json','./icon.svg'];self.addEventListener('install',e=>{e.waitUntil(caches.open(STATIC_CACHE).then(c=>c.addAll(STATIC_ASSETS)));self.skipWaiting()});self.addEventListener('activate',e=>{e.waitUntil(caches.keys().then(keys=>Promise.all(keys.filter(k=>![STATIC_CACHE,API_CACHE].includes(k)).map(k=>caches.delete(k)))));self.clients.claim()});self.addEventListener('fetch',e=>{const u=new URL(e.request.url);const api=['api.frankfurter.app','v6.exchangerate-api.com','api.coingecko.com'].includes(u.hostname);e.respondWith(api?networkFirst(e.request):cacheFirst(e.request))});async function cacheFirst(req){const c=await caches.open(STATIC_CACHE);const cached=await c.match(req);if(cached){fetch(req).then(r=>{if(r.ok)c.put(req,r.clone())}).catch(()=>{});return cached}const r=await fetch(req);if(r.ok&&req.method==='GET')c.put(req,r.clone());return r}async function networkFirst(req){const c=await caches.open(API_CACHE);try{const r=await fetch(req);if(r.ok&&req.method==='GET')c.put(req,r.clone());return r}catch(e){const cached=await c.match(req);if(cached)return cached;throw e}}
